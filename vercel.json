{
    "name": "swiftearnprime",
    "builds": [
        {
            "src": "server.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/server.js"
        }
    ],

    "headers": [
        { "source": "/api/register", "headers": [{ "key": "Access-Control-Allow-Origin", "value": "*" }] }
      ]
}
